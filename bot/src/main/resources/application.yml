bot:
  test: 1
  telegram-token: ${TELEGRAM-TOKEN}
  base-url-scrapper: http://localhost:8080

server:
  port: 8081

springdoc:
  api-docs:
    enabled: true
    path: /openapi/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui